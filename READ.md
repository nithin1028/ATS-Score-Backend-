# ATS SCORE GENEARTOR 
1. It is Backend file of `ATS` score 
2. Build with python sever 

# Frame Works
1. To build this agent use `Langchain`, `Langgraph`
2. API service we `GOOGLE GEMINI`

# WORKING
This is work completly AI , it acepect the `Resume` from the user 
scan the resume give the `score` based on `skills` , `projects`, `internship`,`cerification`

# Installation
```bash
   pip install -U langchain
```
## clone project 
```bash
   git clone https://github.com/nithin1028/ATS-Score-Backend-.git
```